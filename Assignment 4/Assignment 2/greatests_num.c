void main()
{
	int a=20,b=15,c=10;
	if (a>b)
	{
		if (a>c)
		{
			printf("The greates no. is a %d",a);
		}
		else
		{
			printf("The greates no. is c %d",c);
		}
	}
	else
	{
		if (b>c)
		{
			printf("The greates no. is b %d",b);
		}
		else
		{
			printf("The greates no. is c %d",c);
		}
	}
}