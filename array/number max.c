#include<stdio.h>
void main()
{
	int arr[0],max,i;
	printf("Enter a number: ");
	scanf("%d",&arr[0]);
	for(int i=0; i<max; i++)
   {
	if (arr[i]>max)
     max=arr[i];
    }
     printf("maximum number is :%d",arr[i]);
}